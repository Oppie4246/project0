pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                cd estate-agent
                sh 'npm install'
            }
        }
        stage('Deploy') {
            steps {
                sh 'npm start'
            }
        }
    }
}